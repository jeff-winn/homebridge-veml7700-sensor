/**
 * The name of the accessory (which must match the config.schema.json pluginAlias field).
 */
export const ACCESSORY_NAME = 'Homebridge VEML7700 Sensor';

/**
 * The identifier of the plugin (which must match the package.json package name).
 */
export const PLUGIN_ID = 'homebridge-veml7700-sensor';